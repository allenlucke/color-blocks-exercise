import { all } from 'redux-saga/effects';
import { takeEvery} from 'redux-saga/effects';
//Imported sagas


//Watcher Saga
function* rootSaga() {
    yield all(
        [

        ]
    );
}

export default rootSaga;